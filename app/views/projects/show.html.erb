<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<%
=begin%>
 url: project_employees_path(@project)   projects/project_id/employee
<%
=end%>

<%= form_with model:[@project, Employee.new] do |f|%>
  <%= f.select :id, @employees_array%>
  <%= f.submit %>
<% end %>


<% @project.employees.each do |employee| %>
  <p> 
    <%= employee.name %> 
    <%= link_to "X",project_remove_employee_path(@project,employee), method: :delete %> 
  </p>
<% end %> 




<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
